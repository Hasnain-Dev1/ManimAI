# ── Web UI ────────────────────────────────────────────────────
streamlit>=1.35.0

# ── AI (Groq - free tier) ──────────────────────────────────────
groq>=0.9.0

# ── Animation engine ──────────────────────────────────────────
manim>=0.19.0

# ── Manim dependencies (auto-installed with manim, listed for clarity) ──
numpy>=1.23.0
Pillow>=9.1.0
scipy>=1.8.0
colour>=0.1.5
tqdm>=4.62.3
pygments>=2.12.0
rich>=12.0.0
click>=8.0.0
pycairo>=1.21.0
manimpango>=0.4.1

# ── Video processing (used by our ffmpeg fallback logic) ───────
# ffmpeg must be installed separately on the system:
#   Windows : download from https://ffmpeg.org/download.html
#   macOS   : brew install ffmpeg
#   Ubuntu  : sudo apt install ffmpeg